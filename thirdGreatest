The `thirdGreatest(strArr)` function returns the third largest word from the array of strings `strArr`. Words are assumed to contain only letters, and the function returns the last occurrence of the third largest word.


def ThirdGreatest(strArr):
    return sorted(strArr, reverse=True, key=lambda x: len(x))[2]
print(ThirdGreatest(input()))
